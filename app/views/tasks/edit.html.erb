<%= form_with model: @tasks do |l| %>
  <%= l.label :title %><br>
  <%= l.text_field :title %><br>
  <br>
  <%= l.label :details %><br>
  <%= l.text_field :details %><br>
  <br>
  <%= l.submit %>
<br><br>
 <%= l.check_box :completed %><%= l.label :completed %>

<% end %>
<br>
<%= link_to "back to list", tasks_path()%>
